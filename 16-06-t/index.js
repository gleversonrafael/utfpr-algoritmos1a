const a = "Carlos";
const b = "Locars";

console.log(isAnagram(a, b));

function isAnagram(stringA, stringB) {
    let equalToAB = 0;
    
    for(let current = 0; current < stringA.length; current++) {
        if(current)

    }
}